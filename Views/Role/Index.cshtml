@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

<h2>Role Manager</h2>

<p>
    <table class="table">
        <tr>
            <td>
    <a asp-action="CreateRole" class="btn btn-primary">Add New Role</a>
    <a asp-action="Index" asp-controller="UserRoles" class="btn btn-primary">User Roles</a>
            </td>
        </tr>
    </table>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Role Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model)
        {
            <tr>
                <td>@role.Name</td>
                <td>
                    <a asp-action="EditRole" asp-route-id="@role.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@role.Id">Details</a> |                   
                    <a asp-action="DeleteRole" asp-route-id="@role.Id">Delete</a>
                    
                </td>
            </tr>
        }
    </tbody>
</table>

